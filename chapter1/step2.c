/**
  * 1. 첫번째 인수인 2는 반복되고 있고, 두번째 인수는 1씩 증가하고
  * 있으며, 결과값은 이 둘의 곱으로 구할 수 있다.
  * 이렇게 일정한 규칙을 가지고 반복되면 반복문을 사용하여 간결하게
  * 코드를 작성할 수 있다.
void gugudan() {
  for (int index = 0; index < 9; index++) {
    printf("index = %d\n", index);
  }
}
*/

int main(void) {
  gugudan();
  return 0;
}
